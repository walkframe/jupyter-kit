---
title: Reference
description: API reference for every @jupyter-kit package — options, types, and usage examples.
---

API reference for every `@jupyter-kit/*` package. Each page documents the
options exposed by that package, how they plug into the renderer, and any
non-obvious behaviour. The left sidebar lists them by layer:

- **Core & framework wrappers** — the renderer engine and its official
  React / Vue / Svelte / Web Component bindings. Start here.
- **Plugins** — opt-in extensions (editor, math, widgets). Each returns a
  `Plugin` object that you pass to `<Notebook plugins={[…]} />`.
- **Executors** — runtimes for edit-and-run. `executor-pyodide` (Python)
  and `executor-webr` (R) run in the browser; `executor-jupyter` talks to
  a remote Jupyter Server. All three have distinct threat models —
  see [security caveats](#security-caveats-for-executors).
- **Themes** — chrome + syntax CSS bundles. No options; purely stylesheet
  imports.

## Security caveats for executors

Anything that executes arbitrary source code changes the threat model of
your site. The location of execution matters — in-browser executors put
the visitor's device on the hook, remote executors put your server on
the hook. Before shipping any of these on a public page, read the
relevant reference:

- [Pyodide executor — security](/reference/executor-pyodide/#security) —
  arbitrary code in the visitor's browser.
- [WebR executor — security](/reference/executor-webr/#security) —
  arbitrary code in the visitor's browser.
- [Jupyter executor — security](/reference/executor-jupyter/#security) —
  arbitrary code on the Jupyter Server (token = full shell access).

The renderer itself (core + react / vue / wc) sanitises markdown and
HTML/SVG outputs via DOMPurify + `rehype-sanitize` — see the
[XSS security test](https://github.com/walkframe/jupyter-kit/blob/master/packages/e2e/tests/xss.spec.ts)
for the payloads it's verified against.
